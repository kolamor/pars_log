from setuptools import setup, find_packages


setup(name='pars_log',
	version='0.1',
	description='parser log smtp server',
	install_requires=[],
	url='https://github.com/kolamor/pars_log.git',
	packages=find_packages(),
	include_package_data=True)